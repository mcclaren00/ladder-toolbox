---
version: "3"

tasks:

  init:
    desc: initialize precommit hooks
    cmds:
      - pre-commit install --install-hooks

  run:
    desc: run precommit
    cmds:
      - pre-commit run --all-files

  update:
    desc: update precommt hooks
    cmds:
      - pre-commit autoupdate