---
apiVersion: v1
kind: Service
metadata:
  name: multi-service
  namespace: prod
spec:
  selector:
    app: multi
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000 # between 30k and 32k

